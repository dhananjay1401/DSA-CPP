#include<iostream>
using namespace std;

int pairSum(int arr, int n,int s){

for(int i=0;i<arr.size();i++){
    for(int j=i+1;j<arr.size();j++){
        for(int k = j+1; k<arr.size(); k++){
                  if((arr[i] + arr[j] + arr[k]) == s)
{
        };
        };

    };
};
};

int main(){
int arr[5] = {1,2,3,4,5};
int ans = pairSum(arr,5,12)
}
